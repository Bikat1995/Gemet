'use client';
import Link from 'next/link'; import { usePathname } from 'next/navigation'; import { useLanguage } from './LanguageProvider'; import { Icon, type IconName } from './Icons';
const items:[string,IconName,'auctions'|'bids'|'tickets'|'profile'][]=[['/','home','auctions'],['/bids','bids','bids'],['/tickets','ticket','tickets'],['/profile','user','profile']];
export function BottomNav(){const path=usePathname(),{t}=useLanguage();return <nav className="dock nav-safe fixed bottom-0 left-1/2 z-30 flex w-full max-w-md -translate-x-1/2 justify-around px-2 pt-3">{items.map(([href,icon,label])=>{const active=path.startsWith(href) && (href !== '/' || path === '/');return <Link href={href} key={href} className={`dock-item ${active?'dock-active':''}`}><span className="dock-icon"><Icon name={icon} size={20}/></span><span>{t[label]}</span></Link>})}</nav>}
