'use client';
import Link from 'next/link'; import { usePathname } from 'next/navigation'; import { useLanguage } from './LanguageProvider'; import { Icon, type IconName } from './Icons';
const items:[string,IconName,'auctions'|'bids'|'tickets'|'profile'][]=[['/','home','auctions'],['/bids','bids','bids'],['/tickets','ticket','tickets'],['/profile','user','profile']];
export function BottomNav(){const path=usePathname(),{t}=useLanguage();return <div className="dock nav-safe fixed bottom-0 left-1/2 z-30 w-full max-w-md -translate-x-1/2 flex flex-col"><nav className="flex justify-around px-2 pt-3 pb-1">{items.map(([href,icon,label])=>{const active=path.startsWith(href) && (href !== '/' || path === '/');return <Link href={href} key={href} className={`dock-item ${active?'dock-active':''}`}><span className="dock-icon"><Icon name={icon} size={20}/></span><span>{t[label]}</span></Link>})}</nav><div className="mt-1 border-t border-white/10 pt-1 pb-1 text-center text-[8px] text-slate-500/70 font-semibold uppercase tracking-widest">© {new Date().getFullYear()} Gemet. All rights reserved.</div></div>}
